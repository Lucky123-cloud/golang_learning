package main

import (
	"fmt"
)


func main() {
	//for loops in golang acts like the one in c or java
	//we have a loop initialzer(i := 0), condition(i < 5) post(i++)
	for i := 0; i < 5; i++ {
		fmt.Println("Lucky is the best software engineer in the World")
	}
}
